DROP TABLE IF EXISTS note;

CREATE TABLE note (
  id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
  title TEXT NOT NULL,
  date_published DATE DEFAULT now(),
  content TEXT,
  folder INTEGER REFERENCES folder(id) ON DELETE SET NULL
);